name: asesmensmpbm
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  webview_flutter: ^4.4.2
  webview_flutter_android: ^3.13.0
  wakelock_plus: ^1.1.4
  csv: ^6.0.0
  audioplayers: ^5.2.1
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  fl_chart: ^0.66.0 # Tambahkan ini
  battery_plus: ^6.1.0
  camera: ^0.11.0+2
  url_launcher: ^6.3.2
  intl: ^0.20.2
  flutter_windowmanager_plus: ^1.0.1
  file_picker: ^8.0.0
  flutter_math_fork: ^0.7.2
  archive: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/alarm.mp3
    - assets/logo.png


